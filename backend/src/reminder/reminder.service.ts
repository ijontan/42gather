import { Injectable } from "@nestjs/common";
import {Cron, CronExpression} from "@nestjs/schedule";

@Injectable()
export class ReminderService{
	// scheduleJob(date: Date, callback: () => void){
	// 	schedule.scheduleJob(date, callback);
	// }
}